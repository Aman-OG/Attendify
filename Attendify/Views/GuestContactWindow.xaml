<Window x:Class="Attendify.Views.GuestContactWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Contact Administrator" Height="550" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen">

    <Grid Margin="10">
        <!-- Background Blur Layer -->
        <Border CornerRadius="35" Background="#102A35">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.8"/>
            </Border.Effect>
        </Border>

        <!-- Main Content -->
        <StackPanel Margin="30">
            <!-- BACK BUTTON -->
            <Button Content="← Back to Login"
                    Background="Transparent"
                    BorderThickness="0"
                    Foreground="#00A6FB"
                    FontSize="12"
                    HorizontalAlignment="Left"
                    Margin="0,0,0,20"
                    Cursor="Hand"
                    Click="Back_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" 
                                               Foreground="{TemplateBinding Foreground}"/>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>

            <TextBlock Text="Contact Admin"
                       FontSize="28"
                       FontWeight="Bold"
                       Foreground="White"
                       Margin="0,0,0,10"
                       TextAlignment="Center"/>

            <TextBlock Text="Send a request if you don't have an account or need help."
                       FontSize="13"
                       Foreground="#B0FFFFFF"
                       Margin="0,0,0,25"
                       TextWrapping="Wrap"
                       TextAlignment="Center"/>

            <!-- REQUEST TYPE -->
            <TextBlock Text="Request Type" Foreground="White" Margin="5,0,0,5" FontSize="12"/>
            <Border Background="#20FFFFFF" CornerRadius="12" Height="40" Margin="0 0 0 15">
                <ComboBox x:Name="TypeCombo" 
                          Background="Transparent" 
                          BorderThickness="0" 
                          Padding="10,8" 
                          FontSize="14"
                          Foreground="Black"
                          VerticalContentAlignment="Center">
                    <ComboBox.Resources>
                        <Style TargetType="ComboBoxItem">
                            <Setter Property="Foreground" Value="Black"/>
                        </Style>
                    </ComboBox.Resources>
                    <ComboBoxItem Content="Inquiry" IsSelected="True"/>
                    <ComboBoxItem Content="Account Issue"/>
                    <ComboBoxItem Content="Technical Problem"/>
                    <ComboBoxItem Content="Other"/>
                </ComboBox>
            </Border>

            <!-- MESSAGE -->
            <TextBlock Text="Message" Margin="5,0,0,5" Foreground="White" FontSize="12"/>
            <Border Background="#20FFFFFF" CornerRadius="12" Height="150" Margin="0 0 0 30">
                <TextBox x:Name="MessageBox" 
                         Background="Transparent" 
                         BorderThickness="0" 
                         Padding="10,8" 
                         FontSize="14" 
                         TextWrapping="Wrap" 
                         AcceptsReturn="True"
                         Foreground="White"/>
            </Border>

            <!-- SUBMIT BUTTON -->
            <Button x:Name="SubmitBtn"
                    Content="Send Message"
                    Background="#00A6FB"
                    Foreground="White"
                    FontWeight="Bold"
                    Height="45"
                    FontSize="20"
                    BorderThickness="0"
                    Cursor="Hand"
                    Click="SubmitBtn_Click">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="Button">
                                    <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="22">
                                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver" Value="True">
                                            <Setter TargetName="Border" Property="Background" Value="#0088CC"/>
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>
    </Grid>
</Window>
