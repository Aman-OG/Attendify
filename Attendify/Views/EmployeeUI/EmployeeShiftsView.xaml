<UserControl x:Class="Attendify.Views.Employee.EmployeeShiftsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800"
             Background="Transparent">

    <UserControl.Resources>
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>
        
        <!-- Glass Container Style -->
        <Style x:Key="GlassBorder" TargetType="Border">
            <Setter Property="Background" Value="#10FFFFFF"/>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="CornerRadius" Value="15"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="20" ShadowDepth="5" Opacity="0.2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Status Badge Style -->
        <Style x:Key="StatusBadgeStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="8"/>
            <Setter Property="Padding" Value="10,4"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
        </Style>

        <!-- Data Template for Shift Card -->
        <DataTemplate x:Key="ShiftCardTemplate">
            <Border Style="{StaticResource GlassBorder}" Margin="10" Width="320" Height="190">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <!-- Header: Icon + Name -->
                    <StackPanel Orientation="Horizontal" Margin="15,15,15,5">
                         <TextBlock Text="{Binding Icon}" FontSize="24" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="Bold" Foreground="#00A6FB" VerticalAlignment="Center"/>
                    </StackPanel>

                    <!-- Content -->
                    <StackPanel Grid.Row="1" Margin="15,10">
                        <!-- Time -->
                         <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                            <TextBlock Text="⏰" FontSize="14" Margin="0,0,8,0" Foreground="#BBBBBB"/>
                            <TextBlock Text="{Binding DisplayTime}" FontSize="15" FontWeight="SemiBold" Foreground="White"/>
                        </StackPanel>
                        
                         <!-- Grace Period -->
                        <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                             <TextBlock Text="⏱️" FontSize="14" Margin="0,0,8,0" Foreground="#BBBBBB"/>
                            <TextBlock Text="Grace:" FontSize="13" Foreground="#BBBBBB" Margin="0,0,5,0"/>
                            <TextBlock Text="{Binding GracePeriodText}" FontSize="13" FontWeight="SemiBold" Foreground="#4CAF50"/>
                        </StackPanel>

                        <!-- Days -->
                        <StackPanel Orientation="Horizontal">
                             <TextBlock Text="📅" FontSize="14" Margin="0,0,8,0" Foreground="#BBBBBB"/>
                            <TextBlock Text="{Binding DaysText}" FontSize="13" Foreground="#DDD"/>
                        </StackPanel>
                    </StackPanel>

                    <!-- Footer: Status -->
                    <Border Grid.Row="2" Margin="15,0,15,15" 
                            Background="{Binding StatusBackground}"
                            CornerRadius="8" Padding="10,5" HorizontalAlignment="Left">
                        <TextBlock Text="{Binding StatusText}" Foreground="White" FontSize="11" FontWeight="Bold"/>
                    </Border>
                </Grid>
            </Border>
        </DataTemplate>
    </UserControl.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto" Padding="20">
        <StackPanel>
            <!-- Header -->
            <StackPanel Margin="0,0,0,30">
                <TextBlock Text="My Shifts" FontSize="28" FontWeight="Bold" Foreground="White" Margin="0,0,0,5"/>
                <TextBlock Text="Manage and view your assigned work schedules." FontSize="14" Foreground="#AAAAAA"/>
            </StackPanel>

            <!-- Loading Indicator -->
             <TextBlock x:Name="LoadingText" Text="Loading your schedule..." Foreground="#00A6FB" FontSize="16" 
                       HorizontalAlignment="Center" Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}, FallbackValue=Collapsed}"/>

            <!-- Shifts List -->
            <ItemsControl x:Name="ShiftsItemsControl" ItemTemplate="{StaticResource ShiftCardTemplate}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal" />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
            </ItemsControl>

        </StackPanel>
    </ScrollViewer>
</UserControl>