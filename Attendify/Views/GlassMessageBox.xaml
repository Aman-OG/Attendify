<Window x:Class="Attendify.Views.GlassMessageBox"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Message" Width="400"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent"
        WindowStartupLocation="CenterScreen"
        SizeToContent="Height"
        Topmost="True">

    <Grid Margin="15">
        <!-- Background Blur Layer -->
        <Border CornerRadius="25" Background="#EE102A35">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" Opacity="0.6"/>
            </Border.Effect>
            <Border BorderBrush="#30FFFFFF" BorderThickness="1" CornerRadius="25"/>
        </Border>

        <StackPanel Margin="25">
            <!-- HEADER -->
            <TextBlock x:Name="TitleText"
                       Text="Notification"
                       FontSize="20"
                       FontWeight="Bold"
                       Foreground="#00A6FB"
                       Margin="0,0,0,10"/>

            <!-- CONTENT (Wrapped in ScrollViewer for flexibility) -->
            <ScrollViewer MaxHeight="400" VerticalScrollBarVisibility="Auto" Margin="0,0,0,25">
                <TextBlock x:Name="MessageText"
                           Text="..."
                           FontSize="14"
                           Foreground="White"
                           TextWrapping="Wrap"
                           LineHeight="20"/>
            </ScrollViewer>

            <!-- BUTTONS -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="SecondaryButton"
                        Content="Cancel"
                        Width="80"
                        Height="35"
                        Background="Transparent"
                        Foreground="#00A6FB"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="SecondaryButton_Click"
                        Visibility="Collapsed"
                        Margin="0,0,10,0">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Border" Background="Transparent" CornerRadius="8">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#2000A6FB"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button x:Name="PrimaryButton"
                        Content="OK"
                        Width="100"
                        Height="35"
                        Background="#00A6FB"
                        Foreground="White"
                        FontWeight="Bold"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="PrimaryButton_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="10">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#0088CC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
