<UserControl x:Class="Attendify.Views.UserControls.ChangePasswordView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:views="clr-namespace:Attendify.Views"
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="400"
             Background="Transparent">

    <UserControl.Resources>
        <!-- Glass container style -->
        <Style x:Key="GlassContainerStyle" TargetType="Border">
            <Setter Property="CornerRadius" Value="10"/>
            <Setter Property="Background">
                <Setter.Value>
                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                        <GradientStop Color="#10FFFFFF" Offset="0"/>
                        <GradientStop Color="#10FFFFFF" Offset="1"/>
                    </LinearGradientBrush>
                </Setter.Value>
            </Setter>
            <Setter Property="BorderBrush" Value="#20FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Effect">
                <Setter.Value>
                    <DropShadowEffect BlurRadius="18" Opacity="0.18" ShadowDepth="2"/>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern PasswordBox Style -->
        <Style x:Key="ModernPasswordBox" TargetType="PasswordBox">
            <Setter Property="Background" Value="#10FFFFFF"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderBrush" Value="#40FFFFFF"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="12,10"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Grid>
                            <Border x:Name="border" 
                                    Background="{TemplateBinding Background}"
                                    BorderBrush="{TemplateBinding BorderBrush}"
                                    BorderThickness="{TemplateBinding BorderThickness}"
                                    CornerRadius="8">
                                <ScrollViewer x:Name="PART_ContentHost"
                                              VerticalAlignment="Center"
                                              Background="Transparent"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#00A6FB"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Modern Button Style -->
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#00A6FB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="15,12"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"
                                              Margin="{TemplateBinding Padding}"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#0088CC"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="Background" Value="#006699"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter TargetName="border" Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Password Visibility Button Style -->
        <Style x:Key="PasswordToggleButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="Transparent">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>



        <!-- BooleanToVisibility Converter -->
        <BooleanToVisibilityConverter x:Key="BooleanToVisibilityConverter"/>

    </UserControl.Resources>

    <Border Style="{StaticResource GlassContainerStyle}" Padding="25">
        <StackPanel>
            <!-- Header -->
            <StackPanel Margin="0,0,0,25">
                <TextBlock Text="🔐 Change Password" 
                           FontSize="22" 
                           FontWeight="Bold" 
                           Foreground="#00A6FB" 
                           Margin="0,0,0,5"/>
                <TextBlock Text="Update your account password securely"
                           FontSize="13"
                           Foreground="#CCCCCC"
                           TextWrapping="Wrap"/>
            </StackPanel>

            <!-- Current Password -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="Current Password" 
                           Foreground="White" 
                           FontWeight="SemiBold"
                           FontSize="13"
                           Margin="0,0,0,5"/>
                <Grid>
                    <PasswordBox x:Name="TxtCurrentPassword" 
                                 Style="{StaticResource ModernPasswordBox}"/>
                    <Button x:Name="BtnToggleCurrentPassword" 
                            Style="{StaticResource PasswordToggleButton}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"
                            Content="👁️"
                            Click="TogglePasswordVisibility_Click"
                            Tag="Current"/>
                </Grid>
            </StackPanel>

            <!-- New Password -->
            <StackPanel Margin="0,0,0,15">
                <TextBlock Text="New Password" 
                           Foreground="White" 
                           FontWeight="SemiBold"
                           FontSize="13"
                           Margin="0,0,0,5"/>
                <Grid>
                    <PasswordBox x:Name="TxtNewPassword" 
                                 Style="{StaticResource ModernPasswordBox}"/>
                    <Button x:Name="BtnToggleNewPassword" 
                            Style="{StaticResource PasswordToggleButton}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"
                            Content="👁️"
                            Click="TogglePasswordVisibility_Click"
                            Tag="New"/>
                </Grid>
                <TextBlock Text="Minimum 6 characters"
                           FontSize="11"
                           Foreground="#888888"
                           Margin="5,5,0,0"
                           FontStyle="Italic"/>
            </StackPanel>

            <!-- Confirm New Password -->
            <StackPanel Margin="0,0,0,20">
                <TextBlock Text="Confirm New Password" 
                           Foreground="White" 
                           FontWeight="SemiBold"
                           FontSize="13"
                           Margin="0,0,0,5"/>
                <Grid>
                    <PasswordBox x:Name="TxtConfirmPassword" 
                                 Style="{StaticResource ModernPasswordBox}"/>
                    <Button x:Name="BtnToggleConfirmPassword" 
                            Style="{StaticResource PasswordToggleButton}"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Margin="0,0,10,0"
                            Content="👁️"
                            Click="TogglePasswordVisibility_Click"
                            Tag="Confirm"/>
                </Grid>
            </StackPanel>

            <!-- Password Requirements -->
            <Border Background="#1000A6FB" 
                    CornerRadius="8" 
                    Padding="15" 
                    Margin="0,0,0,20">
                <StackPanel>
                    <TextBlock Text="🔒 Password Requirements" 
                               Foreground="#00A6FB" 
                               FontWeight="SemiBold"
                               FontSize="12"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="• Minimum 6 characters"
                               Foreground="#CCCCCC"
                               FontSize="11"/>
                    <TextBlock Text="• Must be different from current password"
                               Foreground="#CCCCCC"
                               FontSize="11"/>
                    <TextBlock Text="• Passwords must match"
                               Foreground="#CCCCCC"
                               FontSize="11"/>
                </StackPanel>
            </Border>

            <!-- Buttons -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                <Button x:Name="BtnCancel" 
                        Content="Cancel" 
                        Style="{StaticResource ModernButton}"
                        Background="Transparent"
                        Foreground="White"
                        BorderBrush="#40FFFFFF"
                        BorderThickness="1"
                        Margin="0,0,10,0"
                        Padding="20,12"
                        Click="BtnCancel_Click"/>

                <Button x:Name="BtnChangePassword" 
                        Content="Change Password" 
                        Style="{StaticResource ModernButton}"
                        Padding="30,12"
                        Click="BtnChangePassword_Click"/>
            </StackPanel>

            <!-- Loading Overlay -->
            <views:LoadingOverlay x:Name="LoadingOverlay" 
                                  Visibility="Collapsed"
                                  Panel.ZIndex="1000"/>
        </StackPanel>
    </Border>
</UserControl>