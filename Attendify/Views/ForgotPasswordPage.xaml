<Window x:Class="Attendify.Views.ForgotPasswordPage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Reset Password"
        WindowStartupLocation="CenterScreen"
        Width="450" Height="550"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    
    <Window.Resources>
        <BlurEffect x:Key="blurEffect" Radius="12"/>
    </Window.Resources>

    <Grid>
        <!-- Background Blur Layer -->
        <Border CornerRadius="35" Background="#CC102A35">
            <Border.Effect>
                <DropShadowEffect BlurRadius="20" ShadowDepth="0" />
            </Border.Effect>
        </Border>

        <Border CornerRadius="35" Padding="40">
            <StackPanel>
                <TextBlock Text="Reset Password"
                           FontSize="28"
                           FontWeight="Bold"
                           Foreground="White"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 10"/>

                <TextBlock Text="Enter your details to reset your password"
                           FontSize="14"
                           Foreground="#DDD"
                           HorizontalAlignment="Center"
                           Margin="0 0 0 30"
                           TextWrapping="Wrap"
                           TextAlignment="Center"/>

                <!-- EMAIL -->
                <TextBlock Text="Email Address" Foreground="White" Margin="5,0,0,5"  FontSize="12"/>
                <Border Background="#20FFFFFF" CornerRadius="12" Height="40" Margin="0 0 0 15">
                    <TextBox x:Name="EmailBox" Background="Transparent" BorderThickness="0" Foreground="White" Padding="10,8" FontSize="14"/>
                </Border>

                <!-- EMPLOYEE CODE -->
                <TextBlock Text="Employee ID" Margin="5,0,0,5" Foreground="White" FontSize="12"/>
                <Border Background="#20FFFFFF" CornerRadius="12" Height="40" Margin="0 0 0 15">
                    <TextBox x:Name="EmpCodeBox" Background="Transparent" Foreground="White" BorderThickness="0" Padding="10,8" FontSize="14"/>
                </Border>

                <!-- NEW PASSWORD -->
                <TextBlock Text="New Password" Margin="5,0,0,5" Foreground="White" FontSize="12"/>
                <Border Background="#20FFFFFF" CornerRadius="12" Height="40" Margin="0 0 0 30">
                    <PasswordBox x:Name="NewPasswordBox" Background="Transparent" Foreground="White" BorderThickness="0" Padding="10,8" FontSize="14"/>
                </Border>

                <Button x:Name="ResetBtn"
                        Content="Reset Password"
                        Background="#00A6FB"
                        Foreground="White"
                        FontWeight="Bold"
                        Height="45"
                        FontSize="20"
                        BorderThickness="0"
                        Cursor="Hand"
                        Click="ResetBtn_Click">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Background" Value="#00A6FB"/>
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <Border x:Name="Border" Background="{TemplateBinding Background}" CornerRadius="15">
                                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                        </Border>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Border" Property="Background" Value="#0088CC"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>

                <Button Content="Back to Login"
                        Background="Transparent"
                        BorderThickness="0"
                        Foreground="White"
                        Margin="0,20,0,0"
                        Cursor="Hand"
                        Click="Back_Click"
                        FontSize="14">
                    <Button.Style>
                        <Style TargetType="Button">
                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate TargetType="Button">
                                        <TextBlock x:Name="Txt" Text="{TemplateBinding Content}" 
                                                   Foreground="{TemplateBinding Foreground}"/>
                                        <ControlTemplate.Triggers>
                                            <Trigger Property="IsMouseOver" Value="True">
                                                <Setter TargetName="Txt" Property="TextDecorations" Value="Underline"/>
                                                <Setter TargetName="Txt" Property="Foreground" Value="#00A6FB"/>
                                            </Trigger>
                                        </ControlTemplate.Triggers>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>
                        </Style>
                    </Button.Style>
                </Button>
            </StackPanel>
        </Border>
    </Grid>
</Window>
