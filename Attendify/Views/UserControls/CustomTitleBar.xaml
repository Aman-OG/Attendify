<UserControl x:Class="Attendify.Views.UserControls.CustomTitleBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="40" Background="#010D2A">
    
    <UserControl.Resources>
        <!-- Button Styles -->
        <Style x:Key="TitleBarButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="45"/>
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Border" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="Border" Property="Background" Value="#40FFFFFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="TitleBarCloseButtonStyle" TargetType="Button" BasedOn="{StaticResource TitleBarButtonStyle}">
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E81123"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Border Background="#200C2B38" BorderBrush="#20FFFFFF" BorderThickness="0,0,0,1">
        <Grid>
            <!-- Draggable Area -->
            <Rectangle Fill="Transparent" MouseLeftButtonDown="TitleBar_MouseLeftButtonDown"/>

            <StackPanel Orientation="Horizontal" Margin="15,0,0,0" VerticalAlignment="Center" IsHitTestVisible="False">
             
                <TextBlock x:Name="TitleText" 
                           Text="Attendify" 
                           Foreground="#B0FFFFFF" 
                           FontSize="14" 
                           VerticalAlignment="Center" 
                           Margin="5,2,0,0"/>
            </StackPanel>

            <!-- Window Controls -->
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Stretch">
                <Button x:Name="MinimizeBtn" Content="–" Style="{StaticResource TitleBarButtonStyle}" Click="Minimize_Click"/>
                <Button x:Name="MaximizeBtn" Content="▢" Style="{StaticResource TitleBarButtonStyle}" Click="Maximize_Click"/>
                <Button x:Name="CloseBtn" Content="X" Style="{StaticResource TitleBarCloseButtonStyle}" Click="Close_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</UserControl>
